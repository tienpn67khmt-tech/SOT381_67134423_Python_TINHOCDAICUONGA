print("Chương trình tính S dùng Hàm (def - return)")

while True:
   
    n = int(input("Nhập số nguyên n (n >= 2): "))
    

    if n >= 2:
        break 
    else:
        print("n phải >= 2 mới tính được. Nhập lại đi!")

ket_qua = tinh_S(n)

print(f"Kết quả S = {ket_qua:.2f}")
